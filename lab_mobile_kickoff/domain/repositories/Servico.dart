class Servico {
  final int id;
  final String titulo;
  final String descricao;

  // Construtor: os nomes entre chaves {} são parâmetros nomeados
  Servico({
    required this.id,
    required this.titulo,
    required this.descricao,
  });
}